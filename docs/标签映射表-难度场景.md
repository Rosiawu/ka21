# 标签映射表（难度 + 场景）

## 目标
- 显性标签：收敛为 `难度 + 核心场景` 双维度
- 隐性标签：保留旧分类/旧标签，作为搜索别名与埋点输入，不在页面显性展示

## 核心场景标签（10个）
- `office-productivity`：办公提效
- `content-writing`：内容写作
- `image-design`：图像设计
- `video-creation`：视频创作
- `audio-podcast`：音频播客
- `coding-development`：编程开发
- `agent-automation`：智能体自动化
- `learning-research`：学习研究
- `commerce-marketing`：电商营销
- `data-analysis`：数据分析

## 映射表位置（完整）
- 工具分类映射：`TOOL_CATEGORY_TO_CORE_SCENARIO`
- 工具标签映射（61 -> 核心场景）：`TOOL_TAG_TO_CORE_SCENARIO`
- 教程分类映射（13 -> 核心场景）：`TUTORIAL_CATEGORY_TO_CORE_SCENARIO`
- 教程技能标签映射（134 -> 核心场景）：`TUTORIAL_SKILL_TAG_TO_CORE_SCENARIO`
- 文件：`/Users/rosiawoo/Desktop/ka21-tools-main/src/lib/coreTaxonomy.ts`

## 隐性兼容策略
- 搜索兼容：旧标签/旧分类通过 `getToolSearchAliasTokens`、`getTutorialSearchAliasTokens` 合并为 alias 词池。
- 埋点兼容：旧标签通过 `hidden_*_tags_sample` 与数量字段上报，供 agent 感知站内细分标签。
- 数据兼容：旧字段不删除，映射层负责投影到核心场景。

## 说明
- 教程与工具的显性标签已统一为：
  - 难度：`小白入门 / 萌新进阶 / 高端玩家`
  - 场景：上述 10 个核心场景
- 新增教程写入时，分类会自动写入核心场景 ID；历史数据通过映射层兼容。
